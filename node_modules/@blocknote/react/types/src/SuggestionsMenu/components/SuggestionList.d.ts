/// <reference types="react" />
import { SuggestionItem } from "@blocknote/core";
export declare type SuggestionListProps<T extends SuggestionItem> = {
    items: T[];
    selectedItemIndex: number;
    itemCallback: (item: T) => void;
};
export declare function SuggestionList<T extends SuggestionItem>(props: SuggestionListProps<T>): JSX.Element;
